"""Alternative FastAPI entrypoint (server.py) for platforms scanning multiple filenames.
Provides `app` variable imported from the core module.
"""
from stellar_platform.serving.api import app  # noqa: F401
